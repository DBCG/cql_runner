import { RunnerDirective } from './runner.directive';

describe('RunnerDirective', () => {
  it('should create an instance', () => {
    const directive = new RunnerDirective();
    expect(directive).toBeTruthy();
  });
});
